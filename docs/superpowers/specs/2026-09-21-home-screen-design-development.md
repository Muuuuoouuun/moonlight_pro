# 첫 화면 디자인 디벨롭 기획 — 두 개의 홈을 하나로

> 상태: **DRAFT · 권장안(운영자 확정 전)**. 본문의 `확정`은 기존 문서·커밋에 이미 남은 운영자 결정만 가리키고, 이 문서가 새로 제안하는 것은 전부 `권장`이다.
> 작성일: 2026-09-21 (Asia/Seoul) · 실측·추가 정리 2026-09-22
> 주의: 이 문서는 브랜치명을 정본으로 적지 않는다(CLAUDE.md). §1.2의 미병합 사실은 작업 시점에 `git log <현재 브랜치>..claude/moonlight-home-futura`로 직접 확인한다 — 2026-09-21 확인 당시 현재 브랜치는 `09.bigmac1.02`였고 2026-09-22에 같은 워크트리가 `09.bigmac1.22`로 옮겨 갔다.
> 상위 정본: [`docs/README.md`](../../README.md) 우선순위 → [`DESIGN.md`](../../../DESIGN.md) §3·§5·§7·§8.1·§11·§13·§15 → [운영자 프로필](../../operator-workflow-profile.md) → [개인 운영 OS 심화 설계](2026-07-13-moonlight-personal-operator-os-deep-design.md)
> 관계: [세 축·Action KPI 기획](2026-09-20-personal-workflow-os-three-axes-and-action-kpi-design.md) §6.2가 첫 화면의 **데이터**(오늘 Top 3 = `tasks.meta.focus_dates`)를 정하고, 이 문서는 같은 화면의 **형태**(슬롯·폴드·텍스처)를 정한다. 둘은 같은 주에 맞물려야 한다 — §7 참조. 2026-09-20 운영자 재확정(캡처 → 오늘 할 일 순서)과 DESIGN.md §15 2026-09-18·19 확정(Futura)을 **둘 다** 지키는 것이 이 기획의 제약이다.
> 근거: 로컬 dev(`:3000`, 라이브 Supabase read)에서 첫 화면 실측 — 데스크톱 1024×768·모바일 375×812 스크린샷과 `getBoundingClientRect()` 슬롯 높이. 코드 읽기: `pages/daily-brief.jsx`(1994줄)·`quick-capture.jsx`·`burning-streak.jsx`·`hub-tokens.css`·`motion.test.mjs`. 미병합 브랜치는 `git show`로 소스를 읽었다 — **`claude/moonlight-home-futura`를 실제로 띄워 보지는 않았다.** 운영 DB 규모·Vercel 배포 상태는 검증하지 않았다.

---

## 0. 한 장 요약

첫 화면은 지금 **두 개**다. 하나는 운영자가 매일 보는 `dashboard`(Daily Brief, 12슬롯)이고, 다른 하나는 운영자가 2026-09-18에 Claude Design에서 5회 이터레이션해 확정한 Futura 트리아지 홈(`dashboard/home`)이다. **후자는 현재 작업 브랜치에 없다.** `09.bigmac1.02`의 DESIGN.md에서 "Futura"는 0회 등장하고, 그 결정과 코드는 23커밋 뒤처진 `claude/moonlight-home-futura`에만 있다.

그래서 이 기획의 첫 문장은 디자인 제안이 아니라 정리다.

1. **확정이 코드에 없다.** DESIGN.md §15의 2026-09-18·19 두 행(Futura 텍스처 레이어 + 셸·공용 컴포넌트 확장)은 `confirmed`인데 현재 브랜치에 병합되지 않았다. 다행히 충돌 면적은 작다 — 그 브랜치가 만진 10파일 중 현재 브랜치와 겹치는 것은 `DESIGN.md`와 `hub-app.jsx` **2개뿐**이다.
2. **현행 첫 화면은 3.34폴드(데스크톱)·4.22폴드(모바일)다.** 실측 2562px / 3430px. DESIGN.md §3-1은 "5초 안에 지금 중요한 것"을, §13은 "12 cards above the fold"를 안티패턴으로 적는다. 슬롯은 정확히 12개다.
3. **가장 큰 슬롯이 가장 덜 말한다.** 633px를 쓰는 「긴급 KA · 집중 고객 · 오늘 일정」 블록에서, 집중 고객 5행은 전부 `다음 행동 대기 · 기약 없음 · 최근 7. 7.`이고 다음 행동 문구는 `lead-enrichment.js`의 템플릿 2종이 돌려 쓴다. 5행이 같은 말을 한다.
4. **팔레트 규칙이 첫 슬롯에서 깨져 있다 — 그리고 첫 슬롯만이 아니다.** 2026-09-22 저장소 전역 스캔 결과 `apps/hub`의 warm(주황~노랑, 채도 0.25 초과) 원색은 **85건**, 원색 danger red까지 더하면 95건이다. 첫 화면 경로(`burning-streak.jsx` 16 · `daily-brief.jsx` 3 · `work.jsx` 5 · `hub-tokens.css` 스트릭 블록 12)가 36건이고, 나머지 49건은 `rhythm-visualizer.jsx`(16) · `revenue-heatmap.jsx`(8) · `celebration-fx.jsx`(6) · `overview.jsx`(4) · `hub-tokens.css` 골드 블록(14)에 있다. DESIGN.md §4는 warm gold/amber 재도입을 금지하고 §9는 라이브 인디케이터 duration을 1.4s 하나로 고정하는데, 넘어간 이유는 명확하다 — `motion.test.mjs`의 정규식이 `\d+ms`만 잡아 **`s` 단위가 통과했고**(무한 애니 `1.8s`·`2.4s` 포함), 팔레트 가드 테스트는 저장소에 **아예 없었다**.

**권장.** 접근안 C — Futura를 병합한 뒤 두 홈을 **한 화면**으로 합치고, 12슬롯을 **6슬롯 · 2폴드**로 줄인다. 트리아지(j/k·1~9)는 "지금 결정할 것" 슬롯 안으로 들어가고, 오늘의 시간표는 Futura에서 그대로 가져온다. 새 원장·새 라우트·마이그레이션은 0이다. 상세는 §5·§8.

---

## 1. 상태 — 실측

### 1.1 현행 `dashboard`(Daily Brief) 슬롯 실측

2026-09-21 09:32 KST, 로컬 dev, 라이브 Supabase read(`status: live`, 기록 8). 높이는 뷰포트 1024×768에서 `getBoundingClientRect()` 실측이다.

| # | 슬롯 | 컴포넌트 | 높이(px) | 실제로 말한 것(그날 데이터) |
|---|---|---|---|---|
| 1 | 헤더 | `BriefClock` + CTA 2 | 85 | `오늘의 실행` · 신호 2 · 즉시 0 · 오늘 0 · [고객 연락][15분 집중] |
| 2 | 상태줄 | `StatusLine` | 20 | `실시간 · 모든 운영 기록 실시간 · 기록 8` |
| 3 | 캡처 | `QuickCaptureForm layout="inline"` | 130 | 입력 1줄 + 할일/정리 전 토글 + 저장 |
| 4 | 오늘 할 일 | `TaskToday` | 407 | 스트릭 배너 + 할 일 4행(놓침 0 · 오늘 1) |
| 5 | 주간 리포트 | `WeeklyReportCard` | 171 | (월요일) 완료 1 · 발행 0 · 연락 0 · 개인 딜 0 |
| 6 | **긴급 KA·집중 고객·오늘 일정** | `FocusSlots` | **633** | 긴급 KA 없음 · 집중 고객 5행(전부 동일 문구) · 일정 3건 |
| 7 | 빠른 이동 | `BriefNavigation` | 72 | 내 작업·캘린더·프로젝트·고객 연락·콘텐츠 칩 5개 |
| 8 | 문의 알림 | `InquirySummary` | 111 | `미확인 0건` |
| 9 | 지금 가장 급한 결정 | `CommandCard` | 170 | 승인 대기 14건 — council 8 · guru 4 |
| 10 | 결정 큐 | `SignalCard` ×N | 121 | 배지 `0 urgent · 0 today` + Content 1건 |
| 11 | 운영 지표 | `MetricCard` ×5 | 336 | MRR ₩12K · PIPELINE ₩56K · PUBLISHED 0 · RUNS FAILED 0 · OPEN WORK 7 |
| 12 | 보조 정보(접힘) | `MoreDetail` | 39 | 리듬 · 모닝 브리프 · 승인 큐 |

- **총 2562px = 3.34폴드**(1024×768) / **3430px = 4.22폴드**(375×812).
- 데스크톱 첫 폴드(768px)에 들어가는 것: 헤더·상태줄·캡처·오늘 할 일 카드까지. 실제 업무 행이 보이는 높이는 702px 중 마지막 407px다.
- 모바일 첫 폴드(812px): 헤더 134 + 상태 44 + 캡처 210 + 갭 60 = **448px(55%)가 입력·크롬**이고, 그 아래 「오늘 할 일」 카드는 스트릭 배너를 먼저 그린 뒤라 **할 일 행이 2행 미만** 보인다.

### 1.2 미병합 `dashboard/home`(Futura 트리아지)

`claude/moonlight-home-futura` `ab87550` — 3커밋, 10파일.

| 커밋 | 내용 | 상태 |
|---|---|---|
| `d6350f8` | `hub-futura.css`(313줄) + `pages/home.jsx`(312줄) — Home A 트리아지 + 오늘의 시간표 | DESIGN.md §15 2026-09-18 `confirmed` |
| `1261095` | Futura 질감을 사이드바·공용 컴포넌트(Button·Input·SegmentedControl·Card)·Decisions로 확장 | §15 2026-09-19 `confirmed` |
| `ab87550` | 사이드바는 **외형만** — 하위 펼침·탑바 탭 숨김 되돌림 | 운영자 지시로 되돌림 |

설계 골자(소스 확인):

- **원장을 새로 만들지 않는다.** `/api/hub/daily-brief`의 `signals`와 `/api/calendar/google/event`를 그대로 소비한다. `SIGNAL_TARGETS` 표도 `daily-brief.jsx`와 같은 값으로 복사돼 있다.
- **분할 트리아지**: 좌측 큐 목록 + 우측 상세 카드. `j`/`k` 이동, `1`~`9` 결정 단축키, `보류` 버튼. 처리하면 로컬 `resolved` Set에서 빠지고 상단 진행바가 `done/total`로 찬다.
- **오늘의 시간표**: 종일 제외·시작시각 정렬, 지난 일정은 `data-past`, 진행 중이면 `NOW`.
- **텍스처 토큰**: `--fx-page-pad: 48` / `--fx-section-gap: 44` / `--fx-card-pad: 26` / `--fx-hero: 44px·weight 300` / `--fx-stat: 38` / `--fx-pill: 999px` / 2단 그림자 `--fx-shadow`·`--fx-shadow-lift`.
- **의도적으로 뺀 것**: 원안의 카테고리 색(붉은 `REVENUE` 라벨)과 Tweaks 밀도 토글 — 각각 §5.3과 §15 2026-07-21 고정 밀도 결정을 뒤집기 때문.
- **기본 착지는 바꾸지 않았다**: `dashboard` → 여전히 daily-brief.

### 1.3 두 화면의 관계 — 지금은 "정해지지 않음"

| 축 | `dashboard` (Daily Brief) | `dashboard/home` (Futura) |
|---|---|---|
| 렌즈 | 브리핑 — 12개 슬롯을 한 번에 편다 | 트리아지 — 신호 하나씩 처리한다 |
| 입력 | 캡처가 3번째 슬롯 | 없음 |
| 할 일 | `TaskToday` 4행 | 없음 |
| 신호 | 명령 카드 1 + 큐 2 + 더보기 | 전량을 큐로 |
| 일정 | `FocusSlots` 안의 목록 | 전용 `fx-timeline` |
| 고객 | 긴급 KA + 집중 고객 5 | 없음 |
| 텍스처 | 1px 하이라인 · 고정 밀도 | 2단 그림자 · pill · 44/26 여백 |
| 밀도 | 2562px / 12슬롯 | 신호 수에 비례(그날 데이터로는 2건) |

**둘 다 운영자 확정이다.** 09-20 재확정(캡처 → 오늘 할 일)은 Daily Brief의 순서를 정했고, 09-18 확정은 Futura를 정했다. 지금 상태로는 한쪽이 다른 쪽을 대체하는지, 공존하는지가 **어디에도 적혀 있지 않다**. 이 문서의 §4가 그 질문에 답한다.

---

## 2. 진단

증거가 있는 것만 적는다. 각 항목의 근거는 §1 실측 또는 인용한 파일이다.

### D1 — 12슬롯 · 3.3폴드는 "5초"와 양립하지 않는다

DESIGN.md §3-1: "첫 화면은 5초 안에 '지금 중요한 것'을 답한다." §13 안티패턴: "Overpacked dashboard with 12 cards above the fold."

지금 12슬롯 중 **그날 실제로 행동을 요구한 것은 3개**였다(오늘 할 일 1건, 승인 대기 14건, Content 1건). 나머지 9슬롯은 0이거나, 변하지 않거나, 같은 말을 반복했다. 이것은 코드 품질 문제가 아니라 **누적 문제**다 — 슬롯은 한 번에 하나씩 정당한 이유로 들어왔고(각 슬롯 위에 달린 주석이 그 이유를 성실히 적고 있다), 아무도 나가지 않았다.

### D2 — 같은 것을 세 군데서 센다

- 헤더 칩: `신호 2 · 즉시 0 · 오늘 0`
- 결정 큐 배지: `0 urgent · 0 today`
- 명령 카드: `승인 대기 14건 — council 8 · guru 4`

헤더의 "신호 2"와 결정 큐의 "0 urgent / 0 today"는 **다른 축을 세면서 라벨이 닮았다**. 운영자는 어느 숫자가 무엇인지 먼저 판단해야 한다.

코드는 이미 이 중복과 싸우고 있다. `daily-brief.jsx`에는 `approvalPromoted`(승인 대기가 신호로 올라오면 헤더 요약에서 빼기)와 `focusUrgentCount`(확정 슬롯의 긴급 KA를 헤더 "즉시"에 더하기)라는 **중복 상쇄 로직이 둘이나** 들어 있고, 그 옆 주석은 "위에서 자리를 받은 것을 아래에서 또 세면 첫 화면 숫자가 다시 검증 불가가 된다"고 적는다. 방어 로직이 필요하다는 것 자체가 구조 신호다.

### D3 — 가장 큰 슬롯이 가장 덜 말한다

`FocusSlots` 633px(전체의 24.7%). 그중 집중 고객 5행의 실제 텍스트:

```
1 윤유경플러스학원  → 구매·수금 이력 확인 → 활성 사용 상태와 갱신·업셀 기회 정리
                     다음 행동 대기 · 기약 없음 · 최근 7. 7.
2 Studysync        → 구매·수금 이력 확인 → 활성 사용 상태와 갱신·업셀 기회 정리
                     다음 행동 대기 · 기약 없음 · 최근 7. 7.
3 메티우스 수학     → 최근 접점 후속 확인 → 활용 상태와 갱신·업셀 기회 정리
                     다음 행동 대기 · 기약 없음 · 최근 7. 7.
4 갈무리국어        → 고객 활성 상태 확인 → 갱신·휴면 여부 정리
                     다음 행동 대기 · 기약 없음 · 최근 7. 7.
5 한밝국어학원      → 고객 활성 상태 확인 → 갱신·휴면 여부 정리
                     다음 행동 대기 · 기약 없음 · 최근 7. 7.
```

다음 행동 문구는 `apps/hub/lib/sales-os/lead-enrichment.js`의 `resolveNextAction` 템플릿 5종(그중 `won` 분기 3종)이 돌아가며 나오고, 메타 줄은 5행 모두 동일하다. DESIGN.md §10 카피 톤("운영자 목소리 — 짧고 구체적이고 방향이 있다")에 비추면 이 문장들은 운영자의 말이 아니라 **필러**다. 5행 중 4행을 지워도 정보 손실이 없다.

### D4 — 지표 5타일 중 2개가 0/0이다

`PUBLISHED 0 · 3 drafts`, `RUNS FAILED 0 · 0 runs`. DESIGN.md §12: "Hub — every metric paired with status or next action." 0 옆에 다음 행동이 없으면 그 타일은 매일 같은 자리를 차지하며 아무것도 바꾸지 않는다. 336px는 모바일에서 거의 반 폴드다.

(2026-07-15 §2.2 QA 결정 B가 "매출 pulse는 접힌 뒤가 아니라 상시 노출"로 이 슬롯을 지켰다. 그 결정은 **매출 축**에 대한 것이지 5타일 전체에 대한 것이 아니다 — §5.2에서 분리한다.)

### D5 — 팔레트·모션 규칙이 첫 슬롯에서 깨져 있다

`apps/hub/components/hub/burning-streak.jsx`:

저장소 전역 스캔(2026-09-22, `apps/hub`, `.next*` 제외, 색상환 hue 15~70 · 채도 0.25 초과):

| 파일 | warm 원색 | 성격 |
|---|---|---|
| `components/hub/burning-streak.jsx` | 16 | 화염 그라디언트·버닝 배지 |
| `components/hub/hub-tokens.css` | 26 | 스트릭 키프레임 12 + 골드 마일스톤 14 |
| `components/hub/rhythm-visualizer.jsx` | 16 | 성과 점수 차트 계열 + 버닝 어휘 |
| `components/hub/pages/revenue-heatmap.jsx` | 8 | 히트맵 색계열 |
| `components/hub/celebration-fx.jsx` | 6 | 축하 골드 |
| `components/hub/pages/work.jsx` | 5 | 버닝 발동 배지·리듬 행 |
| `components/hub/pages/overview.jsx` | 4 | 스파클 |
| `components/hub/pages/daily-brief.jsx` | 3 | 스트릭 인라인 |
| `lib/brand-content-log.js`(+테스트) | 7 | **§15 2026-09-01 확정 예외** — 브랜드 아이덴티티 팔레트 |
| **합계(예외 제외)** | **85** | |

여기에 원색 danger red 10건(`rgba(224, 86, 74, …)` 등 — 토큰 `var(--danger)`를 하드코딩한 사본)이 더해져 총 95건이다.

| 위반 유형 | 근거 규칙 |
|---|---|
| warm amber/orange/gold 원색 85건 | §4 "Do not reintroduce: Warm gold / amber / champagne accents" · §5.2 "raw hex/OKLCH 금지" |
| 무한 애니메이션 2종 `mlFlameFlicker 1.8s` · `mlBurnGlow 2.4s` | §9 "Live indicators: `mlMoonPulse 1.4s` — one duration everywhere" |
| `s` 단위 모션 리터럴 10건(`0.15s`·`0.18s`·`0.2s`·`0.8s`·`2.4s`) | 같음 |
| 이모지·게이미피케이션 카피 `할 일 완성 1일째! 🔥` · `✦ 30일 레전드 완주` · `버닝 발동 🔥` | §10 운영자 목소리 · §13 "Decorative icons used as filler" |

**왜 통과했는가.** `motion.test.mjs`의 탐지 정규식은 `const MS = /(?<![\w.-])\d+(?:\.\d+)?ms\b/` — **`ms`만 봤다.** `1.8s`·`2.4s`는 잡히지 않았다. 팔레트(원색 hex/rgba)를 훑는 스윕 테스트는 저장소에 **존재하지 않는다**(`motion`·`focus-ring`·`button-hover`·`state-usage`·`no-mock-data`는 있다). 즉 이 12건은 규칙 위반이 아니라 **가드 공백**의 결과다. 규칙만 고치고 가드를 안 만들면 다음 병합에서 다시 늘어난다(§15 2026-09-16이 raw `ms` 45건에서 이미 배운 교훈).

### D6 — 확정과 코드가 분리돼 있다

DESIGN.md §15에 `confirmed`로 적힌 2026-09-18·19 결정이 현재 작업 브랜치에 없다. 운영자 입장에서 이것은 "디자인을 확정했는데 앱은 그대로"로 보인다. 병합 비용은 낮다 — 겹치는 파일 2개(`DESIGN.md`·`hub-app.jsx`).

---

## 3. 전제 (권장 — 운영자 동의 필요)

- **P1. 첫 화면은 하나다.** 트리아지와 브리핑은 두 라우트가 아니라 한 화면의 두 슬롯이다. 두 홈을 유지하면 §1.3의 "어느 쪽이 맞나"를 매일 판단해야 한다.
- **P2. 새 원장·새 라우트·마이그레이션 0.** Futura 홈이 이미 이 규율을 지켰고, 세 축 기획의 접근안 A도 같다. 화면이 원장을 새로 가지는 순간 숫자가 갈라진다.
- **P3. 슬롯은 예산이다.** 새 슬롯을 넣으려면 기존 슬롯을 하나 빼거나 접는다. 지금의 12는 예산이 없었기 때문에 생겼다.
- **P4. 0은 자리를 갖지 않는다.** 값이 0이고 행동이 없으면 렌더하지 않는다 — 단, **read 실패는 0이 아니다**(CLAUDE.md 읽기 봉투: `status:"error"`는 반드시 표면화). 현행 `긴급 KA 없음 · KA 지정은 Sales Ledger 시트에서 관리` 같은 "부재 고지"는 이 규칙의 예외로 남긴다(2026-08-05 재감사 #1 — 없는 기능을 있는 척하지 않기).
- **P5. 텍스처는 결정이 끝난 뒤 넓힌다.** Futura는 지금 첫 화면·사이드바·공용 컴포넌트까지 확정이다. 나머지 40여 페이지로의 확장은 이 문서의 범위가 아니다(Q133).

---

## 4. 접근안 비교

| | A. 정리만 | B. 트리아지로 교체 | **C. 병합 후 한 화면(권장)** |
|---|---|---|---|
| 하는 일 | daily-brief의 슬롯만 줄인다. Futura 브랜치는 그대로 둔다 | `dashboard` 기본 착지를 Futura `home`으로 바꾸고 daily-brief는 `dashboard/brief`로 남긴다 | Futura를 병합한 뒤 daily-brief를 Futura 텍스처로 재구성하고 `home`의 트리아지·시간표를 그 안의 슬롯으로 흡수한다 |
| 09-18 확정 | **어긴다**(Futura가 계속 미병합) | 지킨다 | 지킨다 |
| 09-20 확정(캡처→할 일) | 지킨다 | **어긴다**(트리아지에 캡처·할 일 없음) | 지킨다 |
| 코드량 | S | S(라우팅만) — 대신 기능 손실 | M |
| 위험 | 확정이 계속 코드 밖에 남는다 | 매일 쓰는 캡처·할 일·고객이 첫 화면에서 사라진다. 신호 2건짜리 트리아지 화면은 대부분의 날 비어 있다 | 한 화면을 크게 고친다 — 회귀 표면이 넓다 |
| 되돌리기 | 쉬움 | 쉬움(라우팅) | 보통(슬롯 단위로 되돌릴 수 있게 설계) |

**권장 C.** 이유 셋.

1. **두 확정을 모두 지키는 유일한 안이다.** A는 09-18을, B는 09-20을 어긴다.
2. **트리아지는 화면이 아니라 상호작용이다.** `home.jsx`가 실제로 더한 것은 레이아웃이 아니라 `j`/`k`·`1`~`9`·진행바라는 **처리 리듬**이다. 그것은 「지금 결정할 것」 슬롯 안에서 그대로 산다. 반대로 신호가 2건인 날 화면 전체를 트리아지에 내주면 빈 화면이 된다.
3. **Futura의 값이 밀도 문제를 직접 겨냥한다.** 48/44/26 여백과 44px 라이트 히어로는 슬롯 수를 줄이지 않으면 성립하지 않는다 — 텍스처 병합과 슬롯 감축은 같은 작업의 앞뒤다.

---

## 5. 권장 설계 — 새 첫 화면

### 5.1 폴드 계약

| 폴드 | 데스크톱(≥1024) | 모바일(375) |
|---|---|---|
| 1 | 헤더 · 캡처 · **오늘 3개** · 오늘의 시간표 | 헤더(축약) · 캡처 · **오늘 3개** |
| 2 | 지금 결정할 것(트리아지) · 고객 | 오늘의 시간표 · 지금 결정할 것 |
| 3 | (월·목) 주간 카드 · 매출 pulse · 보조 정보(접힘) | 고객 · 주간 · pulse · 보조 정보(접힘) |

**완료 기준은 숫자로 고정한다**: 데스크톱 1024×768에서 전체 **≤2.0폴드(1536px)**, 모바일 375×812에서 **≤3.0폴드(2436px)**. 현행 3.34 / 4.22에서 각각 40% · 29% 감축이다.

### 5.2 슬롯 — 전/후

| 현행 12 | 권장 6 | 처리 |
|---|---|---|
| 1 헤더(칩 3) | **1 헤더** | `fx-eyebrow` 날짜 + `fx-hero` 한 줄. 칩 3개 → **문장 하나**. 0인 축은 문장에서 뺀다(P4). CTA는 `15분 집중` 하나만 남기고 `고객 연락`은 슬롯 5의 헤더로 내린다 — §5.2 "한 뷰에 시각적으로 지배적인 primary CTA 하나" |
| 2 상태줄 | (헤더에 흡수) | `live`면 `TruthBadge` 한 개를 헤더 우측에 인라인. `partial`·`error`일 때만 현행 폭으로 펼친다 |
| 3 캡처 | **2 캡처** | 그대로. 09-20 확정(1순위 입력) 유지. 모바일에서 세로 스택 210px → 압축 검토 |
| 4 오늘 할 일(407) | **3 오늘 3개** | 세 축 §6.2의 `focus` 레인이 1차 소스. 3행 + `나머지 N건 ›`. 스트릭 배너는 §5.4로 재설계 |
| 6 일정(FocusSlots 일부) | **4 오늘의 시간표** | Futura `fx-timeline` 이식. `NOW` 마커·지난 일정 `data-past` |
| 9 명령 카드 + 10 결정 큐 | **5 지금 결정할 것** | 한 슬롯으로 합친다. 상단 1건 상세 + 하단 큐 + 진행바 `done/total`. `j`/`k`·`1`~`9`·`보류`(`home.jsx` 그대로) |
| 6 긴급 KA·집중 고객(633) | **6 고객** | 긴급 KA는 **있을 때만** 렌더(danger 레일 유지). 집중 고객 5 → **3**(심화 설계 확정 범위 `3~5`의 하한) + `더 보기`, 각 행은 보일러플레이트 대신 `마지막 접점 · 다음 연락일` |
| 5 주간 리포트 | 폴드 3 | 월·목 조건부 렌더 유지(Q118·Q119 확정) |
| 11 운영 지표(5타일) | 폴드 3 · **3타일** | MRR · PIPELINE · OPEN WORK만 상시(2026-07-15 결정 B는 매출 축에 대한 것). PUBLISHED·RUNS FAILED는 보조 정보로 |
| 7 빠른 이동 | **삭제(데스크톱)** | 사이드바가 같은 목록을 그린다. ≤900px에서만 유지 |
| 8 문의 알림 | **조건부** | 미확인 0이고 read 성공이면 렌더 안 함(P4). `error`면 반드시 표면화 |
| 12 보조 정보 | **유지(접힘)** | 리듬 · 모닝 브리프 · 승인 큐 + 강등된 2타일 |

순서는 운영자 확정(`docs/README.md` §2: 할 일 → 매출 → 메시지 → 기획 → 콘텐츠)과 어긋나지 않는다 — 할 일이 3번, 매출/고객이 6번, 메시지(문의·승인)가 5번이다.

### 5.3 텍스처 — Futura를 첫 화면에 적용할 때의 경계

| 쓰는 것 | 쓰지 않는 것 |
|---|---|
| `--fx-page-pad/section-gap/card-pad`(48/44/26) — **첫 화면 스코프 안에서만** | 기존 40여 페이지로의 확장(Q133) |
| `--fx-hero` 44px/300 — §11에 Daily Brief 히어로 카브아웃이 **이미 있다**(현재 문면은 `27–32px/700`). Futura 값을 쓰려면 그 카브아웃의 스케일을 44px/300까지 넓히는 §15 결정 1행이 필요하다 | 두 번째 히어로. 페이지당 히어로는 1개 |
| `--fx-shadow`/`--fx-shadow-lift`, `--fx-pill` | 1px 하이라인 계약 폐기 — 상태 레일(`inset 1px 0 0 var(--*-line)`)은 §8.1 그대로 |
| `--fx-stat` 38px — `.stat`(sans tabular) 클래스와 함께 | `.mono`로 된 대형 숫자(§6 하이브리드 숫자 규칙) |
| `.hub-row`/`.hub-card-link`/`.hub-btn` no-JS hover 계약 | 새 `onMouseEnter/Leave` |

### 5.4 스트릭 — 유지하되 브랜드로 되돌린다

세 축 기획 §7.2가 `연속 완주`를 Action KPI로 쓰므로 **지표 자체는 남긴다.** 바뀌는 것은 표현이다.

| 현행 | 권장 |
|---|---|
| 화염 SVG + 앰버 그라디언트(원색 8) | 7칸 도트 스트립 — 완료일은 `--moon-200` 채움, 미완료는 `--line`. 색이 아니라 **형태**가 정보를 나른다(§5.2 "Status color is never sufficient alone") |
| `mlFlameFlicker 1.8s infinite` + `mlBurnGlow 2.4s infinite` | 무한 애니메이션 제거. 완료 순간의 1회 전이만 `--dur-enter`로 |
| `할 일 완성 3일째! 연속 버닝 중 🔥` | `3일 연속 · 오늘 2건` (§10 운영자 목소리) |
| `✦ 30일 레전드 완주` / `✦ 2주 챔피언` | 마일스톤은 라벨 없이 도트 스트립의 길이로. 굳이 적으면 `30일 연속` |

§15 2026-09-16의 선례를 따른다 — 더 긴 축하 연출을 원하면 임의 리터럴이 아니라 토큰(`--dur-celebrate`)을 §15에 추가하는 것이 맞다.

### 5.5 상태 문법(§5.3) 점검표

새 슬롯이 지켜야 하는 것만 추린다.

- 긴급 KA는 화면에서 **유일한 danger 레일**이다(§5.3 red-budget: 폴드당 1). 오늘 할 일의 `놓침` 배지는 danger를 쓰되 레일은 쓰지 않는다 — 현행 `laneTone` 그대로.
- 오늘 3개가 비었을 때 제안하는 3건은 `CertaintyBadge state="recommended"`(dashed `◇ 권장`)로 렌더한다. 실선으로 그리면 운영자가 고른 3개와 구분되지 않는다(세 축 §6.2).
- 트리아지 슬롯의 `보류`는 lifecycle `waiting`이지 certainty가 아니다. `LifecycleBadge state="waiting"` + 사유.
- 시간표의 `NOW`는 lifecycle `active` — 중립. 녹색 금지(§5.3 "done은 neutral, not green").
- 읽기 실패는 전 슬롯에서 `TruthBadge state="error"` + 원인 + 재시도. 빈 목록으로 위장 금지(CLAUDE.md 읽기 봉투).

### 5.6 모바일

- 첫 폴드에 **캡처와 할 일 행 3개**가 함께 들어가는 것이 목표다. 현행은 448px(55%)가 크롬이라 할 일이 2행 미만 보인다.
- 헤더는 모바일에서 `fx-hero`를 27px로 내리고 인사말 줄을 뺀다(`greetingFor`는 데스크톱 전용).
- 캡처는 세로 스택 210px → 입력 1줄 + 저장 버튼 인라인, 목적지 토글은 저장 후 노출.
- 빠른 이동 칩은 ≤900px에서만 유지(사이드바가 오버레이라 2차 내비가 없다).
- 터치 타깃 44px 플로어(§11)와 세그먼트 가로 유지(CLAUDE.md) 계약 유지.

---

## 6. 제거·강등 목록 (근거와 함께)

지우는 것에는 이유를 적는다 — 각 슬롯은 한때 정당한 이유로 들어왔다.

| 대상 | 들어온 이유 | 빼는 이유 |
|---|---|---|
| 헤더 칩 3개 | 화면 전체 요약(2026-08-05 사용성 재감사 A) | 결정 큐 배지와 축이 겹쳐 D2를 만든다. 요약은 문장 하나로 충분 |
| 빠른 이동 칩(데스크톱) | 모바일 점프 칩(§7 fold 순서에 끼어들지 않게 확정 슬롯 아래) | 데스크톱에는 사이드바가 있다. 모바일에서는 유지 |
| 문의 알림(0건일 때) | 미확인 문의 알림(09-12 통합 문의) | 0에 자리를 주지 않는다(P4). error는 반드시 표면화 |
| 집중 고객 4·5행 | 심화 설계 "집중 고객 **3~5건**"(§2 첫 화면 · §3-3) | 5행이 같은 문구다(D3). **확정 범위의 하한을 쓰는 것**이라 결정을 뒤집지 않는다 — 기본 3 + `더 보기 N`으로 5까지 |
| PUBLISHED·RUNS FAILED 타일 | 운영 지표 상시 노출(2026-07-15 결정 B) | 그 결정은 매출 pulse에 대한 것이다. 0/0 타일에는 다음 행동이 없다(D4·§12) |
| 화염 SVG·무한 애니 | 완주 동기 | §4 warm amber 금지 · §9 단일 duration(D5). 지표는 도트 스트립으로 남는다 |
| `dashboard/home` 라우트 | Futura 트리아지 착지 | 같은 화면의 슬롯으로 흡수(P1). `NAV_TREE`·`hub-nav.js` 항목과 `LEGACY_REDIRECTS` 정리 필요 |

---

## 7. 세 축 기획과의 접속

[2026-09-20 세 축·Action KPI 기획](2026-09-20-personal-workflow-os-three-axes-and-action-kpi-design.md)과 **같은 주에 맞물린다.** 그 문서가 데이터를, 이 문서가 형태를 정한다.

| 세 축 §6.2 (데이터) | 이 문서 (형태) |
|---|---|
| `tasks.meta.focus_dates` 배열, 3건 상한 | 슬롯 3 「오늘 3개」가 1차 소스 |
| `focus → missed → today → waiting → inbox` 레인 | 슬롯 3의 정렬. `focus` 밖은 `나머지 N건 ›`로 접는다 |
| 비었을 때 Q116 상위 3개를 dashed `◇ 권장` | §5.5 certainty 규칙 |
| `MorningBriefCard` 소스 교체 | 현행 `MorningBriefCard`는 보조 정보 안에 있다 — 슬롯 3으로 승격하며 **카드가 아니라 소스만** 가져온다 |
| 주간 카드 stat 확장(`focusRate` 등) | 폴드 3 주간 카드. 목표 대비는 색 없이 텍스트·순서로(§7.3) |
| 저녁 리뷰의 읽기 전용 2줄 | 첫 화면 밖(하루 리뷰 팝업) — 이 문서 범위 아님 |

**순서 규칙**: 세 축의 `focus_dates` 계약(주 1)이 먼저 들어가야 슬롯 3이 실제 데이터를 갖는다. 그 전에는 슬롯 3을 현행 `buildTaskToday` 상위 3건으로 렌더하고 `◇ 권장`을 단다 — 화면을 먼저 바꿔도 거짓말은 하지 않는다.

---

## 8. 구현 순서와 완료 기준

전부 `권장`이며, 순서는 의존성 기준이다. **새 테이블·마이그레이션 0.**

> **진행(2026-09-22).** 주 1의 두 줄 — 가드 신설과 스트릭 중립화 — 을 먼저 끝냈다(미커밋).
> 신규 `components/hub/palette.test.mjs`(warm hue 스윕 + 파일별 BASELINE 래칫 + §15 2026-09-01 브랜드 로그 예외),
> `motion.test.mjs`의 `s` 단위 확장(`0s` 제외, 축하 연출 2종은 Q134까지 부채로 명시).
> 정리한 파일: `burning-streak.jsx`(화염 → `StreakMark` 중립 기하) · `rhythm-visualizer.jsx`(차트 계열을 §5.3 명도+점선으로) ·
> `work.jsx` · `daily-brief.jsx` · `hub-tokens.css`(스트릭 키프레임 제거) + `s` 단위 모션 리터럴 5건.
> 결과: warm 원색 85 → **49**(전부 BASELINE에 박제), 루트 `npm test` **1537 · 실패 0**,
> 첫 화면·리듬 화면의 **렌더된 warm 색 0건**(`getComputedStyle` 실측)·콘솔 에러 0.
> **Futura 병합은 하지 않았다** — git 브랜치 작업이라 운영자 확인 뒤에 한다(Q132).

| 주 | 묶음 | 규모 | 완료 기준 |
|---|---|---|---|
| 1 | **Futura 병합**: `claude/moonlight-home-futura` 3커밋을 `09.bigmac1.02`로. 충돌은 `DESIGN.md`(§15 행 병합)·`hub-app.jsx`(PAGE_MAP)뿐 | S | `npm test` 통과(특히 `hub-nav.test.mjs`·`state-usage.test.mjs`). `dashboard/home` 렌더 확인. DESIGN.md §15에 09-18·19 두 행이 현재 브랜치에 존재 |
| 1 | **가드 신설**: `palette.test.mjs` — `apps/hub` 전역에서 warm hue 원색(hex·rgb·hsl)과 토큰 밖 raw 색을 막는다. `motion.test.mjs`의 `MS` 정규식을 `s` 단위까지 확장(`--dur-*`·`mlMoonPulse 1.4s` 화이트리스트) | S | 현행 13건이 **실패로 잡히고**, 수정 후 0. 두 테스트가 루트 `npm test` 글롭 안 |
| 1 | **스트릭 중립화**(§5.4): 화염 SVG → 7칸 도트, 무한 애니 2종 제거, 카피 정리 | S | `palette`·`motion` 테스트 통과. `prefers-reduced-motion`에서 변화 없음 |
| 2 | **슬롯 감축**(§5.2): 헤더 칩→문장, 상태줄 흡수, 빠른 이동 데스크톱 제거, 문의 조건부, 지표 5→3, 집중 고객 5→3 | M | 1024×768 실측 **≤1536px**. 각 변경이 독립 커밋이라 슬롯 단위 되돌리기 가능 |
| 2 | **결정 슬롯 통합**: 명령 카드 + 결정 큐 → 「지금 결정할 것」 하나. `home.jsx`의 `j`/`k`·`1`~`9`·진행바 이식 | M | 키보드 계약 테스트(입력 요소·드로어 위에서 미발화, §8.1). 신호 0건일 때 빈 상태가 `EmptyState`(Skeleton 아님) |
| 3 | **시간표 이식**: `fx-timeline`을 `FocusSlots`에서 분리해 슬롯 4로. Futura 텍스처를 첫 화면 스코프에 적용 | M | 종일 제외·정렬·`NOW` 동작. 캘린더 미연결이면 `TruthBadge preview` |
| 3 | **`dashboard/home` 정리**: 라우트를 첫 화면으로 흡수하고 `NAV_TREE`·`hub-nav.js`·`LEGACY_REDIRECTS` 갱신 | S | `hub-nav.test.mjs` 갱신. `dashboard/home`이 첫 화면으로 리다이렉트 |
| 4 | **모바일 폴드 계약**(§5.6) + 세 축 `focus` 레인 접속(§7) | M | 375×812 실측 **≤2436px**, 첫 폴드에 캡처 + 할 일 3행. `focus_dates`가 있으면 슬롯 3이 그것을 1차 소스로 |

공통 규칙: CLAUDE.md의 Hub read 실패 봉투(HTTP 200 + `status:"error"`), write guard, 목업 금지, primitives-first(§8.1), 모션 토큰(§9), 커밋은 만진 파일만 명시 경로로.

---

## 9. 지금 하지 않는 것

- Futura 텍스처의 나머지 40여 페이지 확장(Q133) — 첫 화면에서 한 달 살아본 뒤.
- 밀도 토글 부활(§15 2026-07-21 고정 밀도 확정).
- 카테고리 색 도입(원안의 붉은 `REVENUE` 라벨 — §5.3).
- 첫 화면 전용 원장·새 API·새 최상위 탭.
- 푸시·알림 채널(세 축 Q130에서 별도 결정).
- 사이드바 깊이 변경 — 2026-07-15 "한 단계" 확정이 살아 있고 `hub-nav.test.mjs`가 고정한다.
- AI 위젯(`FloatingMentorWidget`) 배치 변경 — Council/Guru는 보류 스코프(README §4).

---

## 10. 미정 — 운영자 결정 대기 (Q132~)

세 축 기획의 Q127~Q131 뒤에 붙인다.

1. **Q132 첫 화면 단일화.** 두 홈을 한 화면으로 합칠까(권장 C), 트리아지를 별도 라우트로 남길까? 남긴다면 기본 착지는 어느 쪽인가?
2. **Q133 텍스처 확장 범위.** Futura를 첫 화면·사이드바·공용 컴포넌트까지(현재 확정)로 둘까, 전 페이지로 넓힐까? 넓힌다면 §7 고정 밀도(4·8·12·16·24·32·48)와 44/26의 충돌을 어떻게 정리할까?
3. **Q134 스트릭.** 중립 팔레트로 재설계(권장)·완전 제거·현행 유지 중 무엇인가? 현행 유지라면 §4 warm amber 금지 조항에 예외 행을 §15에 남겨야 한다.
4. **Q135 집중 고객 행의 내용.** 템플릿 문구 대신 무엇을 볼까 — `마지막 접점 + 다음 연락일`(권장)·`최근 활동 1줄`·`딜 금액·단계`?
5. **Q136 운영 지표.** 3타일 상시(권장)·5타일 유지·전부 보조 정보로 강등 중 무엇인가?
6. **Q137 폴드 목표.** 데스크톱 ≤2.0 / 모바일 ≤3.0폴드를 완료 기준으로 고정할까, 다른 수치로 할까?

---

## 11. 내가 본 것

- 12개 슬롯 위에는 **12개의 성실한 주석**이 있다. "여기서 적는데 목록이 9번째 슬롯에 있어서 보이지 않았다", "위에서 자리를 받은 것을 아래에서 또 세면 숫자가 검증 불가가 된다", "보류 스코프가 히어로 CTA를 점유하던 것을 코어 루프로 교체". 하나하나가 옳은 수정이었다. 그런데 **나간 슬롯은 없다.** 첫 화면의 문제는 나쁜 결정이 아니라 예산 없는 좋은 결정들의 합이다.
- 운영자는 2026-09-18에 Claude Design에서 **5회 이터레이션**해 홈 디자인을 확정했다. 그 확정은 3일 뒤인 지금도 매일 보는 화면에 없다. 디자인 결정이 브랜치에 갇히는 것은 디자인 문제가 아니라 통합 문제이고, 이번 병합 비용은 파일 2개다.
- 가장 큰 슬롯(633px)이 같은 문장을 다섯 번 말하는 동안, 그날 실제로 행동을 요구한 것은 세 개였다. 「집중 고객 5」는 "다섯을 보여줄 수 있다"는 기능이지 "다섯이 중요하다"는 판단이 아니다.
- 🔥 이모지와 `✦ 30일 레전드 완주`는 DESIGN.md가 첫 줄부터 막으려던 것이다 — "A strategist's desk, not a startup template." 이것도 누가 잘못한 게 아니라, **팔레트를 지키는 테스트가 없었기 때문에** 들어왔다. 모션은 45건이 쌓인 뒤 가드가 생겼고(§15 2026-09-16), 팔레트는 아직 그 단계 전이다. 13건일 때 가드를 만드는 편이 싸다.
