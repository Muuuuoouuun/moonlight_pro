# Studio 단순화 — "Threads 글 한 편 끝내기"

> 상태: **운영자 확정 · 구현(2026-09-23)**
> 관계: `2026-09-12-content-notes-drafts-ai-workflow-design.md`의 **화면 배치**(좌 편집 / 우 도구 패널, 상시 노출 컨트롤)를 대체한다. 그 스펙의 데이터 모델·저장/복구/충돌 계약·AI 후보 비교/적용·버전 복원은 그대로 유효하다.
> 운영자 지시: "핵심적이고 간단한 것만 하면 되는데 계속 번거롭게 일이 늘어나거나 쾌적하지가 못해" → "Threads 기본으로 바로 구현" · "계속 붙는 것들 정리하자"

## 1. 문제

09-12 이후 Studio 한 화면에 원문·기획 6칸, 채널 선택·추가, 버전 저장·기록, 내보내기, 다음 행동·막힌 이유, 일지·목표 연결, 방향 검토·검수 게이트, AI 작업 5종×말투 4종, 적용 기준, 글쓰기 집중 토글, 저장 상태 7종 문구, 축하 연출이 **상시** 노출됐다(입력·버튼 20개 이상). 기능이 추가될 때마다 패널이 붙었고 빠진 적은 없다.

운영자 실제 흐름(`operator-workflow-profile.md` §12)은 `아이디어 → 선택 → 제작 → 다듬기 → 발행`이며 주력은 Threads다.

## 2. 기본 화면 (확정)

| 영역 | 내용 |
|---|---|
| 헤더 | `원고 작성` · 채널 한 줄 · 조용한 저장 표시 · `새 글 N` · `더보기` |
| 본문 카드 | 제목(선택) → 원문 메모(본문이 비면 펼침) → 본문 편집기(편집/미리보기) → `AI 초안` `AI 다듬기` → `복사` `발행했음` |
| AI 접힘 | `다른 작업 · 말투`: 줄이기·도입부 3안·다른 채널로 변형, 말투, 적용 기준 |

- 채널 기본값은 Threads(`emptyStudioDraft`). 결과물이 2개 이상일 때만 결과물 전환 선택이 본문에 나타난다.
- 저장은 자동(기존 1.2초 디바운스)이며 평소엔 `입력 중`/`저장 중…`/`저장됨` 한 단어. `서버 미연결`·`서버 저장 미확인`·`최신 내용 확인 필요`·불러오기 실패만 `TruthBadge`와 재시도로 올린다.
- 발행 기록은 URL·일시가 서버 계약상 필수라 유지하고, 일시는 지금 시각으로 미리 채운다.

## 3. 표면 예산 (재발 방지)

- 헤더 버튼: 저장 재시도(실패 시에만) · 새 글 · 더보기 — 3개.
- 본문 카드 컨트롤: 제목 · 원문 메모 · (결과물 전환) · 복사 · 발행했음 — 최대 5개 (+ 편집기와 AI 버튼 2개).
- 사이드 패널(`<aside>`) 없음.
- **새 기능은 `더보기` 드로어나 다른 탭으로 간다.** 본문에 더하려면 먼저 하나를 빼거나 이 절을 고친다.
- `apps/hub/components/hub/pages/content-studio-surface.test.mjs`가 이 예산을 고정한다.

## 4. `더보기` 드로어로 옮긴 것 (기능 삭제 없음)

분류(브랜드·결과물 제목) · 채널(첫 채널 선택·빈 결과물 추가·원본 결과물 열기) · 기록(버전 기록·지금 버전 남기기·파일로 내보내기) · 기획 구체화 6칸 · 이어서 할 일(다음 행동·막힌 이유) · 연결된 일지·목표 · 검토(방향 검토·검수 게이트 → 멘토 위젯).

## 5. 제거한 것

- `글쓰기 집중` 토글 — 기본 화면이 이미 한 단 집중 레이아웃이다.
- 상시 `버전 저장` 주 버튼 — AI 적용 시 자동으로 버전이 남고, 수동 저장은 더보기에 있다.
- Studio의 축하 연출(복사 스파클·발행 컨페티) — 전역 `celebration-fx` 모듈은 다른 표면에서 그대로 쓴다.

## 6. 범위 밖

§2~§5는 서버·DB·저장 큐를 바꾸지 않았다(§7은 예외 — AI 변환 입력 1개와 템플릿 테이블 1개 추가). 브라우저 실사용 확인(모바일 390px 포함)은 운영자 검토가 남아 있다.

## 7. AI 요청 · 템플릿 (2026-09-23 운영자 확정: 서버 DB 저장, 요청문 + 글 틀)

운영자 지시: "ai에게 부탁할 프롬프트 소스 넣을 수 있는 것, 템플릿 정하는 칸 생성".

- **위치:** AI 버튼 위 접힌 `AI 요청 · 템플릿` 한 줄(요약에 선택한 템플릿 이름 또는 `직접 입력`/`없음`). 펼치면 템플릿 선택 · `AI에게 부탁할 것`(최대 2000자) · `이 요청을 템플릿으로 저장`. 기본 화면 예산(§3)은 그대로다 — 접힌 칸이며 AI 직접 버튼은 여전히 2개.
- **템플릿 관리:** `더보기 → AI 템플릿`에서 새로 만들기·편집·삭제. 템플릿 = 이름(60자) + AI 요청문(2000자) + 글 틀(4000자), 둘 중 하나는 필수.
- **템플릿 고르기:** 요청문을 `AI에게 부탁할 것`에 채운다. 본문이 비어 있고 텍스트 형식이면 글 틀도 본문에 채운다. 본문이 있으면 덮어쓰지 않는다.
- **AI 계약:** 요청문은 transform 명령의 선택 입력 `request`로 가서 모델 입력의 `operatorRequest`가 된다. 시스템 지시는 이를 운영자 본인의 작성 요청으로 보고 구성·길이·말투·강조·형식에만 따르게 한다. 사실 규칙·출력 계약·작업·대상 채널은 바꿀 수 없고 근거로 쓰지 않는다(요청한 사실이 원문에 없으면 `missing`). 원문 메모·기획·브랜드 필드는 지금처럼 "데이터이지 지시가 아님"이다.
- **기록:** 실행한 요청문은 AI 작업 기록 `source_snapshot.operatorRequest`에 남고, 같은 요청 ID에서 요청문이 다르면 충돌(재생성 없음)로 처리한다. 요청문이 비면 기존 요청 해시와 프롬프트가 바이트 단위로 같다. 콘텐츠 항목에는 요청문을 저장하지 않는다(작업마다 바뀌는 지시문이라서).
- **저장소:** `content_prompt_templates`(마이그레이션 `20260923_0045`), 허브 `GET/POST /api/hub/content/templates`(세션 게이트 + write guard), 저장소 `lib/repositories/content-templates-ledger.js`(revision 낙관적 잠금, 클라이언트 UUID로 재시도 중복 판정). **운영 DB에 `npm run db:migrate` 적용 전에는 `템플릿 저장소 연결 필요`로 표시**되고 요청문은 그 작업에만 쓰인다.

## 8. 사용성·디자인·실용성 감사 반영 (2026-09-24)

서브 에이전트 3개(사용성·편의성 / 방향성·디자인 / 실용성·정합성)가 최신 브랜치를 브라우저(1440·390)와 코드로 점검했다. 반영한 것:

- **회귀 수정:** 90863dd의 `generate()`가 지역 변수 `request`로 같은 이름의 prop을 가려 모든 AI 버튼이 TDZ 오류로 멈췄다. 명령 조립을 `buildStudioTransformCommand`(순수 함수)로 분리해 단위 테스트했고, `content-studio-ai.test.mjs`가 AI 패널을 실제로 렌더한다(선언 순서 실수는 빌드가 아니라 실행에서만 드러난다). 이를 위해 `postStudio`를 `content-studio-api.js`로 분리했다.
- **복사 상시 노출:** 본문이 있으면 복사·발행했음 바가 카드 하단에 sticky. 빈 초안에서는 붙지 않는다(비활성 바가 AI 버튼을 가리지 않게).
- **원고가 주인공:** 본문 `--surface`·`--line-soft`·15px·자동 높이, 제목은 문서 제목처럼(hover/focus에만 하이라인, radius는 `--r-sm`). 접힘 요약 문법 통일(13/500, 1px 쉐브론), 더보기 ghost, 휴지 `--line-strong` 제거, '확인 필요'는 dotted, 후보가 여럿이면 적용 버튼 outline.
- **원문 메모:** 펼침 기본값은 문서가 바뀔 때만 정한다(입력 중 접혀 레이아웃이 튀던 문제). ⌘↵로 바로 AI 초안.
- **브랜드 표시:** 헤더 한 줄에 `채널 · 브랜드`(미지정이면 `브랜드 미지정`) — AI '브랜드 말투'의 근거가 보이게. 설정은 더보기 그대로.
- **발행:** 열 때마다 지금 시각, 다른 결과물이면 URL 비움, `type=url`·Enter 저장, 오류는 드로어 안에만. 기록 뒤 `새 글`·`다음 소재 고르기` 이어가기, 헤더 저장 상태에 `발행 기록됨`.
- **새 글:** 현재 글 저장이 확인되지 않아 열지 못하면 이유를 알린다.
- **AI 요청:** 요약 줄에 지금 갈 요청(24자)을 보여 주고, 이 브라우저에 마지막 요청을 기억한다(편의용 localStorage). 선택 영역은 미리보기 전환·새 글·채널 전환에서 비운다. 생성 중 안내를 "제목·메모·본문을 고치지 않으면 바로 적용"으로 정확히.
- **템플릿:** 마이그레이션 전 테이블 없음은 preview로, 글 틀만 있는 템플릿은 입력한 요청을 지우지 않음, 충돌 시 목록 갱신 후 재저장.
- **Threads 미리보기:** 빈 줄 기준 연결 글 번호 표시(엔진 계약과 일치).

운영자 결정이 필요한 것(구현하지 않음):

1. 빈 초안에서 sticky 바를 `AI 초안`으로 바꿔 보여 줄지(폰에서 메모 작성 후 AI 초안이 화면 밖).
2. 브랜드 미지정일 때 엔진이 '브랜드 말투' 대신 '담백하게'로 떨어질지.
3. AI 생성 중 편집기를 잠글지(지금은 편집하면 후보가 낡음 처리).
4. Studio에 Futura 텍스처를 들일지 — 감사 권고는 **표준 유지**(글쓰기 필드 `--r-sm` 결정과 대형 제목이 원고와 경쟁).
5. `Card` primitive의 이중 하이라인(인라인 1px 보더 + `--shadow-card`의 1px 링) — 허브 전역 영향이라 별도 변경.
6. 템플릿의 글 틀은 본문으로 들어가므로 AI 초안이 틀을 '데이터'로 볼 수 있다 — 구조 지시는 요청문에 적는 편이 확실하다.
