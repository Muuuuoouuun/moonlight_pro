# 산출물 검증

2026-09-20 · 파일과 데이터의 일관성 검증이며 출처 주장의 진위 검증은 아니다.

## 저장소 보관 검증

실행 명령:

```bash
python3 docs/research/2026-09-20-reference-writing/build-library.py
python3 docs/research/2026-09-20-reference-writing/verify-archive.py
python3 docs/research/2026-09-20-reference-writing/verify-archive.py --standalone
```

- PASS: 통합 콘텐츠 370개·원 목록 연결 401건·중복 ID 0·목록 간 중복 연결 31건을 유지했다.
- PASS: 자료별 출처·확인 수준·소속과 원래 요약을 내부 스냅샷과 대조했다. YouTube 265편 중 요약 264편, Instagram 저장 85건·DM 표본 20건을 유지했다.
- PASS: 선별 26개·자막/캡션 구간 대조 22개·기존 AI 응답 검토 4개의 구분을 유지했다. Threads 프로필 2개·발견 표본 5개는 별도다.
- PASS: CSV 370행·15열·수식 접두문자 보호, 글감 6개·초안 3개·소재 카드 1개를 확인했다.
- PASS: 원 수집 스냅샷 17개의 내부 파일 크기·SHA-256이 최초 manifest와 일치한다. 생성기와 검증기는 당시 원본 폴더를 읽지 않는다.
- PASS: 최상위 문서·생성 목록의 상대경로, 명시 앵커, 코드 행번호 링크를 확인했다. 해시로 고정한 원본 스냅샷의 외부 로컬 링크 1개는 역사 기록으로 제외했다. `--standalone`은 저장소 외부로 가는 코드·제품 문서 링크도 별도 제외하며 그 수를 출력한다.
- PASS: 필수 입력만 격리 디렉터리에 복사하고 외부 파일 읽기·네트워크를 차단한 상태에서 재생성한 4개 파일이 보관본과 바이트 단위로 일치했다. 손상시킨 스냅샷은 생성기가 거부했다.
- PASS: 최초 폴더의 32개 파일이 모두 남아 있고 `ARCHIVE.md`·`verify-archive.py` 두 파일만 추가했다. 370개 자료 객체는 과거 위치 필드 이름(`raw_file` → `original_location_at_review`) 외에 값이 같다. `coverage.json`, `library.csv`, Instagram 인사이트 JSON은 최초 보관본과 바이트가 같다. `library.md`는 제목·제작자 표시 줄 끝의 공백만 생성 시 제거했으며 자료 내용·ID·링크는 유지했다. 원본 스냅샷의 공백은 해시 보존을 위해 바꾸지 않았다.

이번 검증은 `evidence-manifest.json`의 14개 이력과 선별 데이터의 연결·해시 형식을 확인했다. `/tmp`에 있던 자막·영상 분석 응답 원본은 복사하거나 열지 않았으므로, 그 파일들의 현재 존재나 SHA-256을 재검증했다고 주장하지 않는다.

## 최초 수집·선별 당시 검증 기록

아래는 저장소로 옮기기 전에 남긴 검증 결과다. 당시 로컬 원본과 임시 파일이 있을 때의 대조 기록이며 이번 재검증 범위를 뜻하지 않는다.

- PASS: 고유자료370개·중복ID0·원목록연결401건
- PASS: YouTube265편 중 내용요약264편·원문상태 유지
- PASS: 선별26개·자막/캡션 직접대조22개·기존분석만검토4개
- PASS: Threads프로필2개·탐색표본5개 별도분리
- PASS: youtube-insights.json필수필드·고유ID 유효
- PASS: instagram-insights.json필수필드·고유ID 유효
- PASS: CSV370행·15열·수식문자보호
- PASS: 원 수집파일17개 원본/복사본 SHA-256 일치
- PASS: 선별 대조파일14개 SHA-256 일치
- PASS: 370개 출처URL형식·상세파일·앵커 유효
- PASS: 로컬 Markdown 링크1199개·행번호·명시앵커 유효
- PASS: 글감6개·초안3개·소재카드1개
- PASS: 검토에서 발견한 dm-001 설명 교정

별도 내용검토: 서브에이전트가 글감6개·초안3개를 선별 원문 기록과 대조했다. dm-001을 부서별 묶음이 아닌 ECC 설정·스킬 묶음으로 교정했고, 개인 경험 날조와 확인수준 확대의 추가 문제는 발견하지 못했다.

범위 밖: 외부 URL 전체의 현재 접속 성공, 370개 원문의 새 정독·시청, 독립 사실 검증, 운영 DB 적재, Studio 실제 저장 왕복, 게시·발행. 코드 변경이 없어 앱 전체 테스트는 실행하지 않았다. Threads 별첨은 원 작업의 관찰 기록이며 이번 작업에서 원문을 다시 읽지 않았다.
